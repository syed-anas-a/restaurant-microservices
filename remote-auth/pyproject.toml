[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "remote-auth"
version = "0.1.0"
dependencies = [
    "Django",
    "djangorestframework",
    "requests"
]